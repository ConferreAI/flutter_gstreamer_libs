import Flutter

public class FlutterGstreamerLibsPlugin: NSObject, FlutterPlugin {
  // need an empty plugin class to work with flutter build pipeline
  public static func register(with registrar: FlutterPluginRegistrar) {
    // print("flutter_gstreamer_libs plugin registered.")
  }
}